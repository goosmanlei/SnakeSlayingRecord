# 项目协作规则

## 跨会话恢复

- 每次进入项目后，依次阅读 `AGENTS.md`、`KNOWLEDGE.md`、`STATE.md`，再开始处理任务。
- 以当前项目目录中的文档和用户在当前会话中的明确说明为准；不得根据项目名称、相邻目录、旧项目或常识补全业务事实。
- 如需参考其他项目，先确认参考范围与复用边界；未经确认，不继承其业务设定、代码状态、数据、任务状态或运行状态。

## 仓库职责边界

- 本故事特有的后台创作流程、检查点工具及其测试留在故事项目，不因“工程化”或“可复用”而自动加入通用审阅台。
- 通用审阅台只承载展示、审阅、评论和数据管理能力；创作工具通过已有的读取／最终发布接口与它交接，不让审阅台反向依赖创作工具。
- 后台候选稿、构想和工作检查点只保存在本机 `.runtime/`，不进入正式审阅台或公开同步；最终干净稿沿既有流程交付。

## 知识沉淀

- `KNOWLEDGE.md` 只记录经用户确认或由可核验证据确认、且预计可跨会话复用的稳定知识。
- 新信息写入前应区分事实、推断和未知；推断与待确认内容不得写入 `KNOWLEDGE.md`。
- 稳定事实发生变化时，直接更新为当前有效表述，避免同时保留互相冲突的旧结论。
- 重要事实应保留足以回溯的来源说明，例如用户确认、文件路径或验证结果。

## 状态收敛

- `STATE.md` 只保留当前有效状态、待确认项和明确的下一步，不作为过程日志或历史档案。
- 每次完成阶段性工作、获得关键确认或改变下一步后，及时更新 `STATE.md`。
- 已稳定且可复用的信息应迁入 `KNOWLEDGE.md`；已解决、失效或被替代的状态应从 `STATE.md` 删除。
- 会话结束前检查三份文档是否相互一致，并确保未将未确认内容写成事实。
