# 当前项目状态

## 当前有效状态

- 日期：2026-09-21。
- 用户已明确确认任务 `task-20260921-0001` 完成；`.codex-project/tasks.json` 回读为 `completed`。
- 任务 `task-20260921-0002` 已启动，任务账本为 `running`、执行次数 1；依赖任务 `task-20260921-0001` 已完成。未达到九项标准，用户尚未确认，不得调用 `_complete`。
- 通用审阅台 Python 系统版本 `2e7e0b1cdb9113a2faf6c4178a2d93461d6a3e8a` 已公开推送，故事实例版本锁已更新。通用系统实现二级分类菜单、精确资料清理和本地媒体导出/浏览器展示；旧参考工作树保持原状。
- Docker Compose 中 Python 服务和 Nginx 在本机 `127.0.0.1:3000` 运行；已按要求从运行库移除两份文言、陈峰外链、关联评论及 5 条事件和共用 SVG；当前导出为 1 份白话译注、0 评论、0 评论事件、1 个对象/修订。五段译文和词语说明保持；清理快照已公开同步为故事提交 `60a47ea`，从双公开仓库干净克隆、恢复并复导出零差异。
- 浏览器已核验左上角返回无参数首页、故事采编命名、页面自然高度和滚动、演出资料链接、模型与推理强度的联动选项。升级 Schema 3 后再次在编辑现有评论时发起真实 AI 润色，返回“尚未保存”的建议及参考上下文，原评论未改；当前 SYSTEM 选定 `gpt-5.6-sol / medium`，润色密钥环境变量名为 `OPENAI_API_KEY`。工作区可用性不再出现在配置中，“本机运行”页已移除；浏览器已核验显示和保存。
- V1 的 4 资料、1 评论、5 事件快照仅属历史验证，当前导出已按新任务清理；进度见 `VERIFICATION.md`。

## 任务状态

### task-20260921-0001：原始资料审阅台 V1

- 状态：已完成（`completed`）；用户已确认，交付验证见 `VERIFICATION.md`。
- 任务 ID：`task-20260921-0001`。
- 摘要：创建双公开仓库与故事实例，交付《李寄斩蛇》原始资料审阅台 V1。
- 优先级：`P1`；复杂度：复杂；模型：`gpt-5.6-sol`；推理强度：`xhigh`；同项目任务依赖：无。
- 范围：创建并关联两个公开仓库、建立独立故事实例、搜集多版本资料、实现原始资料审阅与可复用评论，以及全部实例业务数据的公开同步与恢复。
- 资料数量要求：至少 3 份有实质差异的资料，同文转载不重复计数；包括原文及可追溯的图片，音频可选。
- 验收环境：本机启动，通过浏览器完成实际审阅，并验证 Codex 读取评论以及导出、公开同步和干净实例恢复。
- 完整目标和 8 项验收标准以 `.codex-project/tasks.json` 中该任务记录为准，已核对与用户确认稿一致。
- 本任务范围不含故事改编和任务管理系统的详细实现；故事扩写已另行发布为 `task-20260921-0002`。

### task-20260921-0002：资料扩充与三个完整扩写方向

- 摘要：扩充《李寄斩蛇》故事采编资料，完成至少三个完整扩写方向。
- 状态：`running`，执行次数 1，未完成；优先级：`P1`；复杂度：复杂；模型：`gpt-5.6-sol`；推理强度：`xhigh`；依赖：`task-20260921-0001`。
- 已完成指定三项资料及其关联对象修订、专属评论/事件和 SVG 的本地及远端当前快照清理；保留白话直译正文与出处。浏览器/API 和干净恢复已核验，见 `VERIFICATION.md`。
- 交付下限：至少 2 个内容有实质差异、可下载播放并附完整整理文本的演绎版本；至少 30 则可追溯小故事、覆盖不少于 8 类可扩写情节；至少 3 个完整扩写方向，每个附 20–30 分钟的时长规划。
- 资料组织：通用审阅台菜单及本地音视频资产协议已实施；隔离实例在浏览器验收两组默认收起、独立展开/收起、正文切换、当前项定位和评论隔离，CLI 回读精确修订。正式实例尚未导入小故事和扩写方向，不能计数。
- 演绎媒体检索已找到南词/快板演出报道及平台播讲候选，但尚无两份通过实际下载、完整听辨、差异核对与公开同步条件的媒体，不能计数；小故事 0/30、扩写方向 0/3、时长规划 0/3。检索范围、候选和不可用原因见本轮 `VERIFICATION.md` 进度段。不自行降低标准。
- 完整目标及九项验收标准以 `.codex-project/tasks.json` 中该任务记录为准；发布后已回读核对全部字段与确认稿一致，原任务记录未改动。

## 故事结构页面：发布前设计审阅

- 用户已要求先在任务发布阶段制作独立 HTML demo，确认设计后再发布实际开发任务；原“仅交付设计文档”的候选任务稿已被替代，本页面任务尚未发布。
- 协作流程已确认：Codex 依据选定改编方向起草结构，用户审阅、修改并确认，再作为剧本创作约束。
- demo 范围已获用户明确确认：只呈现“故事结构”页面，以明确标注的《李寄斩蛇》示例串起“选定改编方向 → 起草结构 → 审阅确认 → 交给剧本创作”。这项确认仅针对演示范围，不代表具体页面设计或任务发布已获确认。
- demo 位于通用审阅台仓库 `/Users/bytedance/codex-path/1007.creative/story-review-desk-python/design/story-structure/dist/index.html`，设计说明见同目录上一级 `README.md`；本机预览为 `http://127.0.0.1:8766/`，HTML 也可直接打开。
- 提议的五个维度为创作边界、人物与冲突、情节骨架、分集节奏、约束总览；支持草稿编辑、审阅意见、预置起草候选、确认快照、方向更新复核与演示约束导出。维度、布局、分集粒度和交互均待用户审阅确认。
- 示例故事及 6 集安排不是已确认的真实改编方案；总时长示例为 24 分钟，对齐当前项目 20–30 分钟的规划。demo 仅保留浏览器本地数据，不调用真实 AI，不改变现有故事业务内容。
- 已完成 HTML 静态与 JavaScript 语法检查，以及确认、修订、引用与新增节点/分集的隔离状态检查；HTTP 入口返回 200，首版已在 Chrome 打开。尚未进行浏览器逐项交互或视口视觉验收。详见 demo 的 `README.md`。
- 最新依赖上下文：`task-20260921-0001` 已完成，`task-20260921-0002` 正在执行但尚未完成。故事结构页面任务仍待方案收敛后发布。

## 后续阶段待明确

- 漫剧的目标受众，以及最终选用方向的主题和风格；本轮扩写时长目标与改编边界已明确，见 `KNOWLEDGE.md`。
- 后续改编采用的主要文本版本；将在资料搜集与审阅后讨论确定。
- 新任务管理系统的详细需求、状态模型、通知渠道、运行环境和验收标准。

## 下一步

- 先审阅故事结构 HTML demo 并按反馈调整；设计达成一致后，展示实际开发任务的摘要、目标、可验证完成标准、同项目 task-id 依赖、优先级、复杂度及推理强度，获得明确发布确认后再调用 `_publish`。
- `task-20260921-0002` 保持 `running`。下一步须取得可用的两份不同演出媒体并逐段听辨，之后继续 30 则素材和三个完整方向；资料数量不足时向用户提交候选与原因，请其决定素材获取边界，未达标前不标完成。
