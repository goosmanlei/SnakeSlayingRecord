# 当前项目状态

## 当前有效状态

- 日期：2026-09-22。
- 用户已明确确认任务 `task-20260921-0001` 完成；`.codex-project/tasks.json` 回读为 `completed`。
- 用户已明确确认任务 `task-20260921-0002` 按调整后的范围完成；任务账本回读为 `completed`、执行次数 1、完成时间 2026-09-22 00:10:54 +08:00。演绎媒体及其转写子项按用户此前许可取消，其余标准已验收。
- 通用审阅台 Python 系统版本 `6b1f253ef1e6e49d0bff6bfa4dbd18934f078040` 已公开推送，故事实例版本锁精确指向它。通用系统提供二级菜单、双栏独立滚动，以及评论浮窗 Esc／点窗外关闭；旧参考工作树保持原状，系统仓库未跟踪的 `design/` 未纳入本次提交。
- Docker Compose 中 Python 服务和 Nginx 在本机 `127.0.0.1:3000` 运行；已按要求从当前运行库移除两份文言、陈峰外链、关联评论及 5 条事件和共用 SVG，保留白话译注。当前导出为 36 份资料（译注 1、小故事 32、扩写 3）、1 条扩写方向评论及 1 条事件、36 个对象/修订；内容提交 `820ea4de2731005dee9fbbe5a78aa0579cdddf5b` 已公开同步，从双公开仓库干净克隆、恢复并复导出零差异。
- 浏览器已核验左上角返回无参数首页、故事采编分组与桌面双栏独立滚动、资料切换不回顶、窄屏目录内滚；评论浮窗 Esc／点正文外收起，以及编辑态收起后重开内容仍在。Schema 3 配置仍为 `gpt-5.6-sol / medium` 与润色密钥环境变量名 `OPENAI_API_KEY`；配置只保存名称而非密钥值。此前真实 AI 润色测试已证实建议不自动覆盖评论；工作区可用性不作为用户配置，“本机运行”页已移除。
- V1 的 4 资料、1 评论、5 事件快照仅属历史验证，当前导出已按新任务清理；进度见 `VERIFICATION.md`。

## 任务状态

### task-20260921-0001：原始资料审阅台 V1

- 状态：已完成（`completed`）；用户已确认，交付验证见 `VERIFICATION.md`。
- 任务 ID：`task-20260921-0001`。
- 摘要：创建双公开仓库与故事实例，交付《李寄斩蛇》原始资料审阅台 V1。
- 优先级：`P1`；复杂度：复杂；模型：`gpt-5.6-sol`；推理强度：`xhigh`；同项目任务依赖：无。
- 范围：创建并关联两个公开仓库、建立独立故事实例、搜集多版本资料、实现原始资料审阅与可复用评论，以及全部实例业务数据的公开同步与恢复。
- 资料数量要求：至少 3 份有实质差异的资料，同文转载不重复计数；包括原文及可追溯的图片，音频可选。
- 验收环境：本机启动，通过浏览器完成实际审阅，并验证 Codex 读取评论以及导出、公开同步和干净实例恢复。
- 完整目标和 8 项验收标准以 `.codex-project/tasks.json` 中该任务记录为准，已核对与用户确认稿一致。
- 本任务范围不含故事改编和任务管理系统的详细实现；故事扩写已另行发布为 `task-20260921-0002`。

### task-20260921-0002：资料扩充与三个完整扩写方向

- 摘要：扩充《李寄斩蛇》故事采编资料，完成至少三个完整扩写方向。
- 状态：已完成（`completed`），执行次数 1；用户 2026-09-22 明确确认后调用指定 `_complete`，账本回读成功。优先级：`P1`；复杂度：复杂；模型：`gpt-5.6-sol`；推理强度：`xhigh`；依赖：`task-20260921-0001`。
- 已完成指定三项资料及其关联对象修订、专属评论/事件和 SVG 的本地及远端当前快照清理；保留白话直译五段正文与出处，并清除旧译注内残留的异文对照提法。最新内容的干净恢复、复导出与浏览器回读已核验，见 `VERIFICATION.md`。
- 调整后交付下限：至少 30 则可追溯小故事、覆盖不少于 8 类可扩写情节；至少 3 个完整扩写方向，每个附 20–30 分钟的时长规划。原标准第 2–3 项及第 8 项的媒体播放/完整性核对随演绎子项取消；其他要求均不变。
- 资料组织：正式实例已导入 32 则来源情节与通俗白话整理、连接位置及原创借鉴，覆盖 10 类；另导入 3 份各约 4,000 字的原创完整扩写及各 25.0 分钟规划。浏览器已核验两组菜单与切换、正文、当前项定位、独立滚动和评论隔离；CLI 回读扩写方向二的精确修订与圈选。双仓公开同步、干净恢复、浏览器回读均已通过。
- 演绎媒体检索已找到南词/快板演出报道及平台播讲候选，但无两份通过实际下载、完整听辨、差异核对与公开同步条件的媒体；在此证据基础上用户允许取消该子项。其他标准仍按原要求核验。
- 原目标及九项验收标准见 `.codex-project/tasks.json` 发布记录；执行中的任务不通过只适用于未执行任务的 `codex.project task mod` 改写，当前用户补充说明在 `KNOWLEDGE.md` 和 `VERIFICATION.md` 记录。

## 故事结构页面：设计已确认，开发任务已发布

- 用户已确认当前独立 HTML demo 的阅读式设计符合预期，并明确授权整理发布开发任务。已发布 `task-20260921-0003`：按已确认设计开发故事结构阅读页，整合统一图文评论与改稿审阅。字段回读一致，状态为 `published`、执行次数为 0；本次未执行该开发任务。
- 优先级 P1，复杂度复杂，模型 `gpt-5.6-sol`、推理强度 `high`；依赖同项目 `task-20260921-0002`。完整目标及九项验收标准见 `.codex-project/tasks.json`。
- 协作流程已确认：Codex 依据选定改编方向起草可阅读的图文结果；用户自由圈选文字、图片或图示评论，Codex 根据意见改编后交回审阅，用户确认后作为剧本创作依据。
- 实现须与当前系统复用、整合为同一套评论能力，在不同场景应用；共同复用评论组件、数据模型、API、状态及事件历史，并保留既有评论和 AI 润色行为。该要求已纳入任务目标及验收标准。
- demo 位于通用审阅台仓库 `/Users/bytedance/codex-path/1007.creative/story-review-desk-python/design/story-structure/dist/index.html`，设计说明见同目录上一级 `README.md`；本机预览为 `http://127.0.0.1:8766/`，HTML 也可直接打开。
- 已确认的页面设计为连续阅读稿，呈现方向与主题、人物塑造、人物关系、空间关系、故事线、时间线。正文没有编辑表单；包含一张内嵌氛围图及四张结构图。
- 支持文字区间评论、图片/图示自由圈选及整图评论、原稿定位、意见修改与解决状态、按稿件保留图文引用，以及预置改稿示例。新写的意见不会被预置演示自动处理，调整后的原意见仍待用户复核。
- 故事正文、图中设定和 24 分钟的段落节奏均为演示，不是已确认的真实改编方案。篇幅对齐项目 20–30 分钟目标，未确定最终分集。图像为内置 imagegen 单次生成；图片与全部代码均内嵌 HTML，可单独打开。仅保留浏览器本地演示数据，不调用真实 AI。
- 已完成语法、静态阅读结构，以及文字引用、区域几何、评论保存、原稿保留、新稿关联和图示导出的隔离检查；本机入口返回 200，阅读版已在 Chrome 打开。尚未进行浏览器逐项圈选或视口视觉验收。详见 demo 的 `README.md`。
- 发布时依赖上下文：`task-20260921-0001` 已完成；截至 2026-09-22，`task-20260921-0002` 也已完成。故事结构任务已发布，等待另行发起执行。

## 后续阶段待明确

- 漫剧的目标受众，以及最终选用方向的主题和风格；本轮扩写时长目标与改编边界已明确，见 `KNOWLEDGE.md`。
- 后续改编采用的主要文本版本；将在资料搜集与审阅后讨论确定。
- 新任务管理系统的详细需求、状态模型、通知渠道、运行环境和验收标准。

## 下一步

- 故事结构页面的设计确认与任务发布已完成；`task-20260921-0003` 的依赖已满足，等待另行发起开发，按已确认设计及统一评论要求验收。
