# 当前项目状态

## 当前有效状态

- 日期：2026-09-21。
- 任务 `task-20260921-0001` 正在执行（`.codex-project/tasks.json` 为 `running`）；用户尚未确认完成，不得调用 `_complete`。
- 通用审阅台 Python 新框架提交 `894e9e7d639a53e266b180bb27f0b28a9ff25f4c` 已推送公开；旧参考工作树保持原状。故事仓库正在同步新的实例版本、Compose/Nginx 入口与 Schema 2 导出。
- Docker Compose 中 Python 服务和 Nginx 已在本机 `127.0.0.1:3000` 长期运行；评论、三份资料与一个可追溯 SVG 保留。本机私有 CA 通过不入库的 `compose.override.yaml` 只读挂载，TLS 校验未关闭。
- 浏览器已核验整体导航、“系统管理 → 系统配置”保存（项目配置升至版本 2）、三份资料、评论关联和 AI 参考上下文；真实 AI 请求成功返回建议且未自动保存。当前仍有 1 条明确为验证用途的评论。
- 新导出 Schema 2 覆盖 3 份资料、1 条评论、4 条评论事件、3 个对象/修订、2 个配置及其事件、SVG。最新版本的公开同步、干净实例恢复、重启持久化及复导出一致性尚待最终核验。

## 当前已发布任务

- 任务 ID：`task-20260921-0001`。
- 摘要：创建双公开仓库与故事实例，交付《李寄斩蛇》原始资料审阅台 V1。
- 优先级：`P1`；复杂度：复杂；模型：`gpt-5.6-sol`；推理强度：`xhigh`；同项目任务依赖：无。
- 范围：创建并关联两个公开仓库、建立独立故事实例、搜集多版本资料、实现原始资料审阅与可复用评论，以及全部实例业务数据的公开同步与恢复。
- 资料数量要求：至少 3 份有实质差异的资料，同文转载不重复计数；包括原文及可追溯的图片，音频可选。
- 验收环境：本机启动，通过浏览器完成实际审阅，并验证 Codex 读取评论以及导出、公开同步和干净实例恢复。
- 完整目标和 8 项验收标准以 `.codex-project/tasks.json` 中该任务记录为准，已核对与用户确认稿一致。
- 故事改编留待后续任务；任务管理系统的详细实现不在本次已发布任务范围内。

## 后续阶段待明确

- 漫剧的目标篇幅、受众、风格和具体改编边界。
- 后续改编采用的主要文本版本；将在资料搜集与审阅后讨论确定。
- 新任务管理系统的详细需求、状态模型、通知渠道、运行环境和验收标准。

## 下一步

- 完成故事仓库同步，并从最新公开提交干净克隆恢复；核对评论/配置/素材及复导出一致性，再通过浏览器和容器重启验收。之后向用户逐项展示证据并询问是否确认完成；获明确确认前保持 `running`，不调用 `codex.project task _complete`。
