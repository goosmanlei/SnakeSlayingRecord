# V1 验证记录

2026-09-21，本机主实例入口 `http://127.0.0.1:3000/`，Nginx 容器代理 Python 容器。当前系统版本固定为 `8e95a3a2f55a2630796b9c6331361a48753aefb3`。所有结果只表示本机与本次公开快照，不代表公网部署。

| 验收点 | 观察到的证据 |
| --- | --- |
| 整体框架与旧版 UI 对照 | 在隔离临时目录从旧版已提交源码 `6425f90` 启动原审阅台 UI fixture（`127.0.0.1:3102`，独立测试数据库），实际浏览旧版“当前工作”“故事创作 → 来源资料”“系统管理 → 系统配置”。据此核对并重做深色六模块左栏、方格纸画布、顶部当前视图、工作链卡片、来源索引与阅读区、系统配置双层导航和右侧评论抽屉。旧 fixture 验收后停止并清理测试进程；旧工作树与业务数据未更改。新系统未实现区明确“待开放”，不伪造旧版进度。系统 `docs/architecture.md` 记录对象/修订/依赖、阶段与配置边界。 |
| 资料与图片 | 浏览器显示 3 份有实质差异的资料：通行本、四库本、白话译注；正文完整，出处、链接、采集日期和“原文/整理”类型可见；可追溯 SVG 对照图载入。 |
| 评论闭环 | 浏览器验收过圈选、保存、编辑、关闭、重开、草稿保留；在隔离实例输入“草稿保留验收（未提交）”后收起、重开，输入仍在，未写入公开快照；随后在隔离实例清除测试草稿。主实例保留 1 条验证用途评论，锚定“东越闽中，有”。原位迁移后增加对象 ID 与精确修订 ID，旧评论事件 4 条均保留，数据库 `foreign_key_check` 为零错误。通用对象评论在隔离单测中跨修订仍关联旧正文，不静默移位。 |
| 系统配置 | 浏览器“系统管理 → 系统配置”显示 SYSTEM/PROJECT/LOCAL；PROJECT 创作背景通过页面保存，版本由 1 升至 2，刷新后保留；SYSTEM 版本 1，LOCAL 仅展示 AI 密钥是否配置，不展示密钥。 |
| AI 润色上下文 | 浏览器先展示故事/创作背景、故事采编阶段、圈选及相邻段落、3 份资料全文和上下文哈希，才启用真实 AI 请求。容器通过本机 CA 只读挂载完成 TLS 校验，真实请求返回“尚未保存”建议，原评论未改；模型建议含额外想法，未替用户采用。 |
| Docker 入口 | `docker compose ps` 中 Python 服务健康、Nginx 绑定 `127.0.0.1:3000`；两服务均设 `restart: unless-stopped`。执行 `docker compose restart` 后浏览器重载仍见 3 份资料和 1 条评论，`/api/comments/context` 返回原句与资料修订哈希。容器使用故事目录绑定卷保存 SQLite；证书校验未关闭。 |
| 自动测试 | `python3 -m unittest discover -s tests -v`：4 项通过，覆盖 Unicode/跨块/并发、旧评论迁移、配置和 AI 请求内容、对象修订评论跨版本关联、完整导出恢复/复导出一致性及篡改拒绝。`node --check` 和 `git diff --check` 通过。 |
| 导出内容 | Schema 3 manifest 计数：3 资料、1 评论、4 评论事件、3 对象/修订、2 配置及事件；`materials.json`、`comments.json`、`objects.json`、`configurations.json`、SVG 均有 SHA-256。 |
| 新版主实例浏览器 | `127.0.0.1:3000` 用 Chrome 实际切换通行本、四库本、白话译注；搜索“白话”得到 1/3 条；“当前工作”依据实例真实数据显示 3 份资料和 1 条待处理评论，卡片可返回资料；系统配置的故事项目/系统与 AI/本机运行分组可切换。评论抽屉显示原句“东越闽中，有”，进入编辑时“查看润色参考”显示故事背景、创作背景、阶段、圈选、相邻段落、3 份资料和哈希；未采用建议，原评论未变。未开放工作区可进入明确提示页。 |
| 双仓公开同步 | `gh repo view` 回读 `goosmanlei/story-review-desk`、`goosmanlei/SnakeSlayingRecord` 均为 `PUBLIC`；`git ls-remote` 核对系统 `8e95a3a2` 与故事实例版本锁一致，故事仓库版本锁提交 `bc60d6ab` 已公开。Python/Nginx 系统代码只在系统仓库。 |
| 最终 Schema 3 公开干净恢复 | 在 `/private/tmp/snake-review-final-draft-iYwG2U/` 从双仓 GitHub HTTPS 地址新克隆，系统 `8e95a3a2`、故事提交 `bc60d6ab`。空实例 `restore` 校验 5 个文件 SHA-256，得到 3 资料、1 评论、4 事件、3 对象/修订、2 配置；`check` 保留故事采编阶段、背景、受众/风格 UNKNOWN；评论仍指向“东越闽中，有”和精确修订 `7740273c…`。复导出后 `git diff --exit-code -- export` 无差异，故事克隆无已跟踪文件差异。前一版干净克隆还通过 `docker compose config --quiet` 和 Nginx 镜像构建。 |
| 最终恢复实例浏览器 | `127.0.0.1:8769` 打开最终克隆恢复实例：3 份资料、1 条原文高亮，SVG `naturalWidth=1000,naturalHeight=660`，评论面板仍关联“东越闽中，有”；前一轮恢复实例还核对过项目配置版本 2、故事/创作背景和故事采编阶段。临时服务验收后停止，长期入口仍是 Nginx 3000。 |

验收边界：长期运行状态和浏览器效果在本机核验；公开同步验证到 GitHub 的仓库可见性与提交，未部署到公网。测试评论是验证数据，不能当作用户意见。

后续用户评论公开同步前应审阅正文；AI 建议可能偏离原意，只在用户手动采用并保存后进入业务数据。
